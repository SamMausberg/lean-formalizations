# E119 Source Note

This file records the section names from the user-supplied informal source note provided on 2026-04-18 for Erdős 119. The Lean declarations in this folder cite these headings directly so that each formal statement points back to the corresponding informal declaration in the source note.

## Section 0. Cited external facts

- Fact 1: top-scale geometric regularity after rotation from a prefix-norm bound.
- Fact 2: local affine-rank control for ordered index sets.
- Fact 3: dyadic band defect lower bound for `u_n(θ)`.
- Fact 5: qualitative bounded-lift transfer of local defect budgets.

## Section 1. Dyadic affine-rank rigidity theorem and false stronger route

- Counterexample: small local gap defect does not force closeness to a parity class.
- Dyadic affine-rank rigidity theorem with local bad-rank sets `E_j`, local budgets `κ_j`, cumulative defect `D_j`, residue chain `R_j`, and nested good sets `G_j`.
- Exact corollary: if the cumulative defect is less than `1`, then the chain is exact.
- Geometric “after rotation” version.

## Section 2. Explicit error propagation across scales

- Recursion `D_{J₁} = 0`, `D_j = D_{j+1} + κ_j`.
- Linear accumulation bound when all local defects are bounded by one number.

## Section 3. Version stable under bounded `q`-lifts

- Structural additive defect-budget formulation in terms of `κ_j`.

## Section 4. Induction input for `W(2^m, σ)` or `S(2^m, σ)`

- Weighted error budget `∑ D_j = ∑ (ℓ - J₀ + 1) κ_ℓ`.
- “Energy versus structure” dichotomy.

## Section 5. Self-audit

- The gap-only theorem is false.
- The additive affine-rank theorem is the repaired conclusion.

## Section 6. Top-scale tradeoff theorem

- Theorem A: a lower bound for `E_{t-1}` under the hypotheses `E_t ≤ ε`.
- Corollary B: the clean small-`ε` lower bound `E_{t-1} ≥ 1 - 2 / t - 16 L^{2/3}`.
- Exact regular example at `ε = 0`.

## Section 7. Fixed-center coherence

- Counterexample showing shell totals alone are insufficient.
- Observation that a shell condition starting exactly at the inner edge cannot work.
- Repaired trimmed fixed-center coherence theorem.
- Coordinated sparse-chain corollary and lower bound for `u_n(θ)` and `M_n`.

## Section 8. Low-moment return decomposition

- Irreducibility bound from quantized prefix states.
- Quantized return decomposition theorem.
- Counterexample to the naive exact bounded-`q`-lift inverse theorem.

## Section 9. `W` versus parity recursion

- Exact `3^m` is false for `W`.
- Explicit `q = 8` counterexample with value `21 + 4u + 2/u`.
- Recursive family with value `ρ 3^m`, `ρ < 1`.
